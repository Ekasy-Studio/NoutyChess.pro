name: One-time online reconnect integration

on:
  push:
    branches: [main]
    paths:
      - scripts/integrate-online-reconnect.mjs
      - .github/workflows/one-time-integrate-online-reconnect.yml

concurrency:
  group: noutychess-online-reconnect
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22.13.0'
          cache: npm

      - name: Apply reconnect integration
        run: node scripts/integrate-online-reconnect.mjs

      - name: Install dependencies
        run: npm ci

      - name: Production dependency audit
        run: npm audit --omit=dev --audit-level=low

      - name: Tests
        run: npm test

      - name: Lint
        run: npm run lint

      - name: Production build
        run: npm run build

      - name: Commit validated integration
        run: |
          git config user.name "Ekasy automation"
          git config user.email "actions@users.noreply.github.com"
          git add components/nouty-chess-game.tsx
          git diff --cached --quiet && exit 0
          git commit -m "Adiciona reconexão preservando a partida online"
          git push
