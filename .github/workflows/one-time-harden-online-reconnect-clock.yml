name: One-time harden online reconnect clock

on:
  push:
    branches: [main]
    paths:
      - scripts/harden-online-reconnect-clock.mjs
      - .github/workflows/one-time-harden-online-reconnect-clock.yml

concurrency:
  group: noutychess-online-reconnect-clock
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22.13.0'
          cache: npm

      - name: Apply reconnect clock hardening
        run: node scripts/harden-online-reconnect-clock.mjs

      - name: Install dependencies
        run: npm ci

      - name: Production dependency audit
        run: npm audit --omit=dev --audit-level=low

      - name: Tests
        run: npm test

      - name: Lint
        run: npm run lint

      - name: Production build
        run: npm run build

      - name: Commit validated reconnect hardening
        run: |
          git config user.name "Ekasy automation"
          git config user.email "actions@users.noreply.github.com"
          git add components/nouty-chess-game.tsx
          git diff --cached --quiet && exit 0
          git commit -m "Mantém partida P2P e relógio seguros durante quedas"
          git push
